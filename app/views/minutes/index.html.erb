<div class="container">
  <h2 class="main-section-title">Meeting Minutes</h2>
  <hr class="medium-underline">
  <% if current_user && current_user.admin? %>
  <div class="bottom-buffer">
    <%= link_to 'Add New Meeting Minutes', new_minute_path, class: 'btn btn-md btn-primary btn-minutes' %>
  </div>
  <% end %>
  <% if @minutes.present? %>
    <% @minutes.each do |year, minute| %>
    <p class="section-title"><%= year %></p>
      <% minute.each do |m| %>
        <div class="col-md-4 col-sm-4 event-details">
          <p class="section-title event-title"><%= m.title %></p>
          <p class="published-date"><%= m.minutes_date %></p>
          <br>
          <span>
            <%= link_to m.pdf_file.url, target: '_blank' do %>
            <%= image_tag 'pdf.png', class: 'event-icon' %>
            <% end %>&nbsp;&nbsp;
            <% if current_user && current_user.admin? %>
            <%= link_to 'Edit', edit_minute_path(m) %>
            &nbsp;&nbsp;
            <%= link_to 'Delete', minute_path(m), method: :delete, data: {confirm: 'Are you sure you want to delete this?'} %>
            <% end %>
          </span>
          <br>
        </div>
      <% end %>
    <% end %>
  <% end %>
</div>
